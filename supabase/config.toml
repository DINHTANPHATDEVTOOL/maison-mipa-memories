# ==============================================================================
# Maison MIPA Memories - Supabase Local & Deployment Configuration
# Project: dkvkhysnabhtbbuvommu
# ==============================================================================

project_id = "dkvkhysnabhtbbuvommu"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

# ------------------------------------------------------------------------------
# Edge Functions Configuration:
# - create-payos-link: verify_jwt = true (LEGACY)
#   Requires authenticated customer/manager session.
#   Authoritative amount is fetched directly from database.
# - send-email: verify_jwt = true
#   Private function for backend notification outbox worker / service role only.
# ------------------------------------------------------------------------------

[functions.create-payos-link]
verify_jwt = true

[functions.send-email]
verify_jwt = true

[functions.request-otp]
verify_jwt = false

[functions.verify-otp]
verify_jwt = false

[functions.google-drive-oauth]
verify_jwt = false

[functions.drive-delivery]
verify_jwt = true
